

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://admin.cw6f368mqpxq.ap-northeast-2.rds.amazonaws.com:3306/shop_db?serverTimezone=UTC&characterEncoding=UTF-8
    username: admin
    password: wlstjd0604
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show-sql: true
        default_batch_fetch_size: 100
        # where in(100) 파라미터 갯수 지정
        # 일대다 N 컬렉션 100개 데이터를 1번 쿼리 실행
        # 데이터가 1000개이면 1000/100= 10번 쿼리 실행
  profiles:
    active: dev





sentry:
  dsn: https://ecf8dde803d84e3dbecde0428608850a@o1205153.ingest.sentry.io/6334921
  traces-sample-rate: 1.0

server:
  port: 8080



# App Properties
shop:
  app:
    jwtSecret: shopSecretKey
    jwtExpirationMs: 60000000
# 6000000
# 600,000 = 10분

cloud:
  aws:
    credentials:
      accessKey: AKIA26ZMMWJ7MPCZKE6E
      secretKey: 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
    s3:
      bucket: shopping-back-s3
    region:
      static: ap-northeast-2
    stack:
      auto: false

logging:
  level:
    org.hibernate.SQL: debug
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error